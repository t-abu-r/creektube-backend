{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py makemigrations && python manage.py migrate && gunicorn burst.wsgi:application --workers 4",
    "restartPolicyType": "ON_FAILURE"
  }
}